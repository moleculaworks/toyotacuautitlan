# Toyota Cuautitlán — Control de Proyecto

## ✅ Completado

### Infraestructura
- [x] Scaffold Next.js 16 con TypeScript + Tailwind CSS
- [x] Instalación de dependencias (Sanity, React Hook Form, Zod, next-seo)
- [x] Colores de marca Toyota configurados — ver `SISTEMA-DE-DISENO.md` para la referencia completa (colores, ancho de página, tipografía)
- [x] Repositorio en GitHub: github.com/moleculaworks/toyotacuautitlan
- [x] Deploy en Vercel: toyotacuautitlan.vercel.app
- [x] Variables de entorno configuradas en Vercel

### CMS — Sanity Studio
- [x] Proyecto Sanity creado (Project ID: tuhugumb)
- [x] Schema: Modelos — detalle completo de campos en `sanity/schemaTypes/modeloType.ts` (no se duplica aquí)
- [x] Schema: Promociones (título, vigencia, modelo relacionado, activa/destacada)
- [x] Schema: Configuración del sitio (teléfono, WhatsApp, dirección, horario, redes, banners)
- [x] Studio disponible en: toyotacuautitlan.vercel.app/studio
- [x] Studio registrado y conectado al proyecto

### Páginas
- [x] Homepage (/) — hero con CTA
- [x] Catálogo (/modelos) — grid con filtros por categoría
- [x] Detalle de modelo genérico (/modelos/[slug]) — plantilla de respaldo simple para modelos sin página dedicada (no confundir con /modelos/corolla, la página rica que es el patrón a replicar)
- [x] Promociones (/promociones) — cards con vigencia
- [x] Cotización (/cotizacion) — formulario validado
- [x] Cita de servicio (/cita-de-servicio) — formulario validado
- [x] Contacto (/contacto) — datos + mapa

### Formularios
- [x] Validación con Zod (cotización y cita)
- [x] API routes /api/cotizacion y /api/cita (listas para conectar CRMs)

### Equipo
- [x] Raúl invitado a GitHub (moleculaworks/toyotacuautitlan) — acceso de escritura confirmado.
      Token de acceso personal (PAT) configurado en el llavero de macOS — `git push` funciona
      sin pedir credenciales cada vez
- [x] Raúl invitado a Sanity (proyecto tuhugumb) — conectado con raulpontones@gmail.com,
      confirmado vía MCP que el proyecto muestra 2 miembros
- [x] Auditoría del repo: sin archivos ajenos al sitio, limpio para clonar

---

## 🔲 Pendiente

### Diseño visual (en progreso)
- [x] Wireframe de página de modelo individual (handoff Corolla 2026)
- [x] Página piloto /modelos/corolla implementada según handoff:
  - Hero full-bleed, intro, highlights, carrusel de 6 versiones,
    selector de 7 colores, TSS, CTA rojo intermedio, seguridad,
    galería con lightbox (exterior/interior), rendimiento, CTA final,
    modelos similares
  - Fuente ToyotaType integrada en todo el sitio, no solo el Corolla (pendiente confirmar licencia web)
- [x] Contenido de texto del Corolla 100% dirigido por Sanity (año, categoría real, textos de sección, íconos de marca) — ya no hay texto/dato fijo en código
- [x] Base de diseño cerrada (22 de agosto): ancho/breakpoints/colores/tipografía consistentes en todo el sitio — ver `SISTEMA-DE-DISENO.md`
- [x] Jerarquía de encabezados (h1-h3) auditada y corregida en todo el sitio — ver `SISTEMA-DE-DISENO.md` §4
- [x] Secciones de la página de modelo se ocultan automáticamente si falta el dato en Sanity (se quitó el `FALLBACK` que mostraba contenido fijo del Corolla) — permite publicar un modelo nuevo con información parcial
- [x] Textos breves de SEO en Versiones/Exterior/Galería (primer paso, no resuelve el tema de contenido delgado por completo)
- [x] Página de modelo convertida a ruta dinámica `/modelos/[slug]` (15 de septiembre de 2026) — antes vivía hardcodeada en `/modelos/corolla`; ahora cualquier modelo cargado en Sanity con el mismo esquema de contenido obtiene la página rica automáticamente. Componentes movidos de `components/corolla/` a `components/modelo/`. Ver `CHANGELOG.md`.
- [x] Catálogo completo de 24 modelos definido (15 de septiembre de 2026), comparando toyota.mx y toyotaoaxaca.com.mx — incluye qué modelos existen, su categoría/precio, y los 5 nameplates con motorización doble físicamente distinta (Corolla, Corolla Cross, Yaris Sedán, Tacoma, RAV4) que requieren dos documentos separados con carrusel de versiones duplicado. Ver Obsidian `catalogo-de-modelos.md`.
- [x] Precios y datos del Corolla corregidos y publicados (15 de septiembre de 2026): LE HEV $515,300 → $524,400, XLE HEV $567,300 → $576,400; XLE CVT/XLE HEV ya no comparten foto; rendimiento corregido a una fila por modelo (CVT 19.14, se quitó la fila HEV con dato viejo de 26.3). Nota: "Quemacocos" se quitó de XLE HEV por no aparecer en la ficha técnica y **se restauró el mismo día** — Raúl confirmó contra fotos de toyota.mx que sí lo trae de serie. Ver `CHANGELOG.md`.
- [x] Campo `imagenPrincipal` renombrado a `imagenTarjeta` en todo el stack (schema, código, dato del Corolla migrado) — el nombre viejo no describía su uso real (miniatura de catálogo/"Modelos similares"). Ver `CHANGELOG.md`.
- [x] `Imagenes Ejemplo Modelo/` renombrada a `Imagenes por Modelo/`, con convención definitiva de carpetas/nombres para los 24 modelos (una carpeta por página, slug siempre como prefijo del archivo). Ver `CLAUDE.md` del proyecto.
- [x] Página de prueba visual reutilizable (`app/preview-borrador/page.tsx`, temporal/local) para revisar el copy de cualquier borrador de `JSON Modelos/` con el diseño real de la página, usando fotos del Corolla como marcador de posición. Pensada para repetirse con los 24 modelos. Ver `CHANGELOG.md`.
- [x] **Corolla HEV publicado en Sanity** (15 de septiembre de 2026) — documento nuevo (slug `corolla-hev`), separado del Corolla CVT, con las 119 imágenes propias ya producidas por Raúl (hero, tarjeta, destacado, rendimiento, 6 versiones, visor 360° de 6 colores × 16 ángulos, galería exterior/interior). Colores del 360° ajustados a los reales de toyota.mx (Blanco Perlado de la ficha técnica no está disponible ahí; se usó Negro en su lugar). Pendiente: deploy en Vercel para que la página exista en el sitio (publicar en Sanity no actualiza el HTML estático — ver `AGENTS.md`).
- [ ] Cargar el resto de los 12 modelos restantes (12/24 publicados: Corolla, Corolla HEV, Camry HEV, Yaris Sedán, Yaris Sedán HEV, Yaris Hatchback, Prius, Avanza, Raize, Corolla Cross, Corolla Cross HEV, RAV4 HEV) (datos desde fichas técnicas + toyota.mx, precio siempre desde toyota.mx; imágenes las trabaja Raúl en Photoshop) — proceso ya probado: borrador en JSON (`JSON Modelos/`) → revisión visual → aprobación → Sanity → publicar
  - [x] **Camry HEV publicado en Sanity** (16 de septiembre de 2026) — segundo modelo del catálogo completado de punta a punta (slug `camry-hev`). Modelo único, sin par CVT/gasolina (en México el Camry solo existe como HEV). 101 imágenes propias producidas por Raúl: hero, tarjeta, destacado, rendimiento, 4 versiones, visor 360° de 5 colores × 16 ángulos (80 imágenes — Blanco y Blanco Perlado comparten las mismas fotos, igual que en toyota.mx), galería exterior/interior (6+6). `destacadoTexto` con viñetas de Toyota Safety Sense desde el primer momento (ya no hubo que migrar como con Corolla). Pendiente: deploy en Vercel para que la página exista en el sitio (publicar en Sanity no actualiza el HTML estático — ver `AGENTS.md`).
  - [x] **Yaris Sedán publicado en Sanity** (16 de septiembre de 2026) — tercer modelo (slug `yaris-sedan`, documento `48ea9a87-6a82-4860-ad55-265d893fa710`). Uno de los 5 nameplates con motorización doble: el carrusel de `versiones` ya incluye 6 tarjetas (5 de gasolina + 1 "HEV" a $450,000) siguiendo el patrón de Corolla/Corolla HEV — cuando se construya el documento "Yaris Sedán HEV", debe llevar las mismas 6. Primer modelo con 2 filas de rendimiento (CVT 18.54 / MT 20.10 KM/L). 135 imágenes propias de Raúl (hero, tarjeta, destacado, rendimiento, 6 versiones, visor 360° de 7 colores × 16 ángulos, galería 6+6) — revisión previa encontró y corrigió un nombre de archivo sin guión (`s-hi-cvt`) y confirmó que la foto de "Base CVT" y "S MT" es intencionalmente la misma (toyota.mx también las muestra idénticas). Pendiente: deploy en Vercel para que la página exista en el sitio (ver `AGENTS.md`).
  - [x] **Yaris Sedán HEV publicado en Sanity** (16 de septiembre de 2026) — cuarto modelo del catálogo (slug `yaris-sedan-hev`, documento `ca984df4-2f37-46eb-8784-cd83b8fc7621`), par HEV del Yaris Sedán con el carrusel de `versiones` duplicado (mismas 6 tarjetas e imágenes reutilizadas por referencia del documento `yaris-sedan`, sin volver a subirlas). Colores del visor 360° leídos de los atributos `data-color1` del DOM (6: Blanco, Rojo, Plata, Escarlata, Gris, Negro — sin Blanco Perlado, a diferencia del Yaris Sedán de gasolina). Primer modelo del catálogo con **36 ángulos por color** en vez de 16 (216 imágenes de 360°, detectado en vivo contra toyota.mx, no asumido) y con imágenes fuente en PNG con transparencia en vez de JPG opaco (compuestas sobre fondo blanco y ajustadas a 1200×800 sin recortar, ya que su proporción real no es 3:2 exacta como en los modelos anteriores). **Sin galería de interior** (decisión del proyecto, no imágenes faltantes) — se ajustó `components/modelo/Galeria.tsx` para ocultar el botón "Interior" automáticamente cuando ese arreglo viene vacío en vez de mostrar un grid en blanco (commiteado y subido a `main`). 226 imágenes totales subidas vía `sanity exec` + `client.assets.upload()` (mismo mecanismo que los 3 modelos anteriores). Imagen de tarjeta corregida después (Raúl había subido por error la del Yaris Sedán de gasolina). Pendiente: deploy en Vercel para que la página exista en el sitio (ver `AGENTS.md`).
  - [x] **Yaris Hatchback publicado en Sanity** (17 de septiembre de 2026) — sexto modelo del catálogo (slug `yaris-hatchback`, documento `yaris-hatchback-modelo`). **No tiene par HEV** (confirmado en `catalogo-de-modelos.md`) — solo 3 versiones: Base CVT ($326,500), S MT ($357,000), S CVT ($383,300). Motor verificado en su propia ficha técnica: 1.5L, 106 HP (distinto a los 105 HP del Yaris Sedán pese a compartir nameplate). 6 colores (Blanco, Plata, Gris, Escarlata, **Cemento** —nombre nuevo en el catálogo—, Negro). **Visor 360° con solo 8 ángulos por color** (no 16), primer modelo del catálogo con este rango más chico, extensión `.jpeg`, verificado con peticiones HEAD sin huecos. Destacado = "Comodidad" ("Cada viaje, una experiencia") en vez de Toyota Safety Sense, decisión explícita de Raúl para variar respecto a los últimos modelos (TSS de este modelo, solo en S CVT, sigue reflejado en la barra de destacados y en la tarjeta de esa versión). 68 imágenes propias de Raúl (incluyendo el visor 360° esta vez, no solo las categorías habituales), revisión previa sin hallazgos (nombres, resoluciones y hashes MD5 correctos de punta a punta — la carga más limpia hasta ahora). Pendiente: deploy en Vercel para que la página exista en el sitio (ver `AGENTS.md`).
  - [x] **Prius publicado en Sanity** (17 de septiembre de 2026) — séptimo modelo del catálogo (slug `prius`, documento `prius-modelo`). Modelo único, 100% HEV, sin par de motorización. **Año 2027** (ficha "MY 27", distinto a los demás modelos con ficha 2026). 2 versiones: Base HEV ($525,500) y Premium HEV ($599,500), 138 HP total (motor 1.8L 97 HP + motor eléctrico 94 HP). **6 colores, no 5** — la ficha técnica solo lista Blanco Perlado/Plata/Gris/Rojo/Azul Oscuro, pero toyota.mx en vivo trae un sexto ("Mostaza") con visor 360° completo; se incluyó dando prioridad al sitio en vivo. Las 16 imágenes de Mostaza llegaron con fondo transparente (fuente PNG de toyota.mx, mismo patrón ya visto en Yaris Sedán HEV) — compuestas sobre fondo blanco por Claude antes de subir. Destacado = "Modos de manejo" (ECO/NORMAL/PWR/EV) tras descartar "Hybrid Synergy Drive" tras pregunta de Raúl: se verificó contra las fichas de Corolla HEV/Camry HEV/Yaris Sedán HEV que HSD es el nombre genérico de Toyota para cualquier híbrido, no algo exclusivo del Prius. Ícono de "Rendimiento" (31.12 KM/L) en la barra de destacados también descartado por el mismo motivo — comparado contra los 6 modelos publicados, el Corolla HEV ya tiene 31.31 KM/L, prácticamente empatado — reemplazado por "Espacio" (2,750mm entre ejes; el título pasó de "Más espacio" a solo "Espacio" en una revisión posterior, ver más abajo). Sensores de estacionamiento con AEB y BSM agregados a Seguridad marcados como exclusivos de Premium. 115 imágenes propias de Raúl, documento armado vía `sanity exec` (mismo mecanismo que los modelos anteriores). Publicado y con deploy en Vercel confirmado.
  - [x] **Avanza publicado en Sanity** (17 de septiembre de 2026) — octavo modelo del catálogo (slug `avanza`, documento `avanza-modelo`), **primero de Suv's & Minivans**. Modelo único, sin par de motorización. Solo 2 versiones (LE MT $367,300 y XLE CVT $400,200) — el modelo con menos versiones del catálogo hasta ahora. Motor 1.5L · 105 HP, 7 pasajeros, 498 L de cajuela con 3ra fila abatida (toyota.mx muestra ese mismo dato mal etiquetado como "487 kg" en su tarjeta "Avanza en números" — se usó el valor correcto de la ficha técnica). **Sin Toyota Safety Sense en ninguna versión** (primer modelo del catálogo sin TSS) — el destacado usa en su lugar el tema de versatilidad/espacio familiar ("Todos viajan con comodidad", copy real de toyota.mx) con los asientos abatibles modo sofá. 2 filas de rendimiento (CVT 19.10 / MT 16.40 KM/L), igual patrón que Yaris Sedán. 99 imágenes propias de Raúl (hero, tarjeta, destacado, rendimiento, 2 versiones, visor 360° de 5 colores × 16 ángulos, galería 6+6) — carpeta de imágenes creada con la subestructura incompleta al inicio (faltaban las subcarpetas `versiones/`, `360/<color>/`, `galeria/`), corregido antes de que Raúl empezara a producir fotos; luego se detectaron y corrigieron 2 archivos de versiones subidos a la raíz en vez de `versiones/` y un typo en el nombre del archivo de destacado (`avanza-detacado.webp` → `avanza-destacado.webp`). Documento completo armado con `create_documents` (todos los campos + 99 referencias de imagen en una sola llamada) en vez del patrón incremental de `patch_documents` usado en modelos anteriores. Publicado y verificado contra `perspective: "published"`.
  - [x] **Raize publicado en Sanity** (19 de septiembre de 2026) — noveno modelo del catálogo (slug `raize`, documento `raize-modelo`), segundo de Suv's & Minivans. Modelo único, sin par de motorización. 3 versiones (XLE MT $390,400, XLE CVT $404,900, XLE CVT Bi-Tono $418,200). Motor Turbo 1.0L · 97 HP — único turbo del catálogo hasta ahora. **Segundo modelo sin Toyota Safety Sense** (después de Avanza) — el destacado usa el área de carga versátil (asientos abatibles + Tonneau Cover, "Agranda su espacio") en vez de TSS. **Primer modelo del catálogo con colores bi-tono** (6 sólidos + 4 combinaciones con techo negro, 10 en total) — se agregó el campo opcional `coloresExterior[].hexSecundario` al schema de Sanity y al componente `ExteriorColores.tsx` (swatch partido en diagonal cuando existe), sin afectar a los 8 modelos ya publicados. Primer modelo con conteo de ángulos del visor 360° **distinto por color dentro del mismo modelo** (5 colores × 8 ángulos, 5 colores × 4 — así los tiene toyota.mx). El copy se revisó en dos rondas con Raúl: se quitó la mención a "variedad de colores" como argumento de venta (el distribuidor no siempre tiene en existencia todos los colores del catálogo) y se reescribió la intro de un tono de ficha técnica a uno de uso real (ciudad/escapadas) — ambas reglas quedaron documentadas en `JSON Modelos/README.md` como convención para el resto del catálogo. 80 imágenes propias de Raúl, documento completo armado con `create_documents`. Publicado con confirmación explícita y verificado contra `perspective: "published"`.
  - [x] **Corolla Cross publicado en Sanity** (19 de septiembre de 2026) — décimo modelo del catálogo (slug `corolla-cross`, documento `corolla-cross-modelo`), primero de los 5 nameplates con motorización doble en construirse desde Raize (par con Corolla Cross HEV, aún no construido). 4 versiones en el carrusel: LE ($547,000) y XLE ($617,900) propias + SE HEV ($631,900) y XSE HEV ($684,900) duplicadas del par HEV. Motor 2.0L · 169 HP, CVT. **Toyota Safety Sense completo** (4 componentes: PCS, LKA, LTA, AHB) como destacado, comparado contra los 4 modelos con TSS ya publicados antes de confirmarlo. **9 bolsas de aire — el número más alto del catálogo hasta ahora** — subido a la barra de 4 destacados, requirió ícono nuevo `seguridad` en `HighlightIcon.tsx` y en el schema. 6 colores sólidos (el bi-tono es exclusivo de XSE HEV, para cuando se construya el HEV). Primer modelo con 18 ángulos por color en el visor 360°. Galería interior con solo 4 fotos (confirmado por Raúl). Intro revisada en 3 rondas: de ficha técnica → enfoque 100% familiar (rechazado, mejor para Sienna) → enfoque general de día a día. 125 imágenes propias de Raúl, `create_documents`, **publicado directo sin pausa de borrador** (nuevo criterio, ver sección SEO/proceso).
  - [x] **Corolla Cross HEV publicado en Sanity** (20 de septiembre de 2026) — décimo primer modelo del catálogo (slug `corolla-cross-hev`, documento `corolla-cross-hev-modelo`), cierra el segundo de los 5 nameplates con motorización doble (después de Corolla/Corolla HEV). Mismas 4 versiones que Corolla Cross duplicadas en el carrusel. Sistema híbrido de **196 HP combinados** (toyota.mx no desglosa cilindrada; la ficha técnica en PDF no se pudo descargar, bloqueada por WAF/Cloudflare en todos los intentos). Cajuela de 349 L — **descartada como destacado** tras comparar contra las SUV's ya publicadas (la más baja del catálogo: Avanza 498 L, Raize 369 L, Corolla Cross 434 L) — el cuarto ícono es Rendimiento (25.88 KM/L) en su lugar. **4 colores, todos bi-tono, sin ningún sólido** (Rojo/Negro, Azul/Negro, Blanco/Negro, Plata/Negro). Destacado con Toyota Safety Sense (5 componentes, incluye Control Crucero Full Speed Range, ausente en el documento de gasolina). Intro y texto de "Diseño" reescritos 2 veces cada uno con Raúl (ver `CHANGELOG.md` para el detalle). De paso se encontró y documentó el tope real de `disenoTexto` en el schema (`r.max(160)`) y se acordó un rango de 120-160 caracteres para ese campo en todo el catálogo (`JSON Modelos/README.md`), pendiente de aplicarse retroactivamente. 91 imágenes propias de Raúl, documento armado con script `sanity exec` + asset upload por CLI, **publicado directo sin pausa de borrador**. `seguridadItems` corregido el mismo 20 de septiembre (incluía BSM de más, copiado sin verificar del gemelo gasolina) al revisar la ficha técnica real, ya disponible localmente en `Fichas Tecnicas/` — no hacía falta pelear con el 403 de toyota.mx.
  - [x] **RAV4 HEV publicado en Sanity** (20 de septiembre de 2026) — décimo segundo modelo del catálogo (slug `rav4-hev`, documento `rav4-hev-modelo`), tercero de los 5 nameplates con motorización doble — único donde ninguna de las dos motorizaciones (HEV/PHEV) es 100% gasolina. 5 versiones en el carrusel: LE HEV ($650,300), XLE HEV ($712,000), Woodland HEV ($816,800) y Limited HEV ($898,500) propias + RAV4 PHEV ($999,000) duplicada. **10 colores**, labels con el formato real de toyota.mx (nombre + versiones entre paréntesis, ej. "Negro (LE / XLE / Limited)", "Blanco (LE / Woodland)") — corregido el mismo día tras notar Raúl que sin paréntesis parecía que "Woodland"/"Limited" era parte del nombre del color. **Primer modelo con Toyota Safety Sense 4.0** — agrega RSA (Asistente de Señales de Tránsito), componente nuevo en el catálogo. Destacado = "Connected Services" (Safety/Remote/Service/Drive Connect + Drive Recorder) en vez de TSS, primera vez que aparece este concepto. **Capacidad de carga en KG** (504 kg, LE) en vez de litros — primer modelo sin dato de cajuela en L. **Primer modelo con rendimiento por VERSIÓN en vez de por transmisión** (27.70/25.30/23.20 KM/L) — Toyota no publica este dato para este modelo; excepción puntual autorizada por Raúl usando expansion.mx y carglobe.mx, no es precedente para el resto del catálogo. De paso se corrigió la línea divisoria de la tabla de "Rendimiento" (antes solo soportaba 2 filas, ahora cualquier número) en `app/modelos/[slug]/page.tsx` y se sincronizó `preview-borrador`, que había quedado desactualizado sin esa línea. 202 imágenes propias de Raúl, **publicado directo sin pausa de borrador**.
- [x] **Reordenamiento de secciones de la página de modelo + rename "Exterior" → "Diseño"** (17 de septiembre de 2026, aplica a los 24 modelos) — probado primero en `app/preview-borrador/`, aprobado por Raúl, aplicado a `app/modelos/[slug]/page.tsx`. Orden nuevo: Hero, Intro, Highlights, Diseño, Galería, CTA intermedio, Destacado, Seguridad, Rendimiento, Versiones, CTA final, Modelos similares — genera deseo (diseño/tecnología/seguridad) antes de mostrar precios, patrón inspirado en toyota.mx. El rename implicó migrar el campo de Sanity `exteriorTitulo`/`exteriorTexto` → `disenoTitulo`/`disenoTexto` en los 5 modelos publicados hasta ese momento (schema + query + plantilla). De paso se corrigió un bug en `VersionesCarousel.tsx`: el contador "1–4 de N versiones" no acotaba el límite superior al total real (detectado con Yaris Hatchback, primer modelo con menos de 4 versiones). Ver `CHANGELOG.md` para el detalle completo.
- [x] **Auditoría y sincronización de `JSON Modelos/` contra lo realmente publicado en Sanity** (17 de septiembre de 2026) — Raúl estableció que la intro (`descripcion`) debe medir 300-350 caracteres; al verificar se encontró que **los 5 archivos locales tenían la intro desactualizada** respecto a Sanity (ej. Yaris Sedán HEV: 128 caracteres locales vs. 336 reales), y que `corolla.json` tenía además las características de las versiones LE HEV/XLE HEV completamente distintas a lo publicado. Los 5 archivos se corrigieron y verificaron campo por campo contra `query_documents` con `perspective: "published"`. **Lección: `JSON Modelos/` puede desincronizarse de Sanity — no es fuente de verdad confiable por sí sola, verificar directo en Sanity cuando haga falta comparar contra algo ya publicado.** Mismo corte de auditoría: 15 imágenes huérfanas borradas de Sanity (3.35 MB, todas rastreadas a correcciones ya documentadas — JPEG pre-conversión del Corolla, tarjeta equivocada del Yaris Sedán HEV, etc.).
- [x] **Segunda depuración de imágenes huérfanas en Sanity** (17 de septiembre de 2026, tras la carga del Avanza y varios reemplazos de imagen) — 19 imágenes sin referencia borradas (1.16 MB), todas rastreadas a los reemplazos de hero (7 modelos) y destacado (Corolla HEV, Yaris Sedán) hechos en esta misma sesión, más 2 sobrantes antiguos sin relación. Confirmado con Raúl antes de borrar (acción irreversible). Mismo mecanismo que la primera depuración (script `sanity exec` comparando `sanity.imageAsset` contra referencias usadas en documentos `modelo`, perspectiva raw).
- [x] **Mega-menú de "Modelos" en escritorio + acordeón en móvil** (17 de septiembre de 2026) — reemplaza el link simple del navbar por el patrón de toyota.mx: panel con las 5 categorías a la izquierda y tarjetas (imagen/nombre/precio) de la categoría activa a la derecha en escritorio (`components/layout/ModelosMegaMenu.tsx`, hover); acordeón anidado Modelos → categoría → tarjetas en scroll horizontal en móvil (`components/layout/ModelosMobileAccordion.tsx`), reemplazando el link simple anterior. Ambos leen `getModelos()` en vivo desde `app/layout.tsx` (ahora async) — crecen solos con los 18 modelos restantes. Categorías compartidas en `lib/categorias.ts` (ya no duplicadas en `app/modelos/page.tsx`). Se decidió **conservar** `/modelos` como respaldo/SEO aunque toyota.mx no tenga página equivalente — se puede reconsiderar más adelante. Tamaños finales tras varias rondas con Raúl: categorías y nombre de modelo 15px/semibold, precio 14px/normal (antes 12px muy claro, ilegible). Probado primero en local (incluye una comparación de encuadre de `imagenTarjeta` con overrides temporales, removidos antes de publicar — ver lección de caché abajo).
  - [x] **Ajuste de espaciado (17 de septiembre de 2026, con el catálogo ya en 8 modelos)** — Raúl notó que las categorías se veían apretadas y las tarjetas de modelo ocupaban demasiado espacio en pantalla. Categorías: `py-3`→`py-5`. Tarjetas: grilla de 3 a 4 columnas (`grid-cols-3`→`grid-cols-4`, `gap-5`→`gap-4`, `sizes="200px"`→`"160px"`). Probado en local antes de subir.
- [x] **Revisión post-publicación del Prius — 6 correcciones, algunas aplican a varios modelos** (17 de septiembre de 2026, sesión siguiente a la publicación):
  1. **"MXN" reemplazado por "M.N." en las 6 ubicaciones donde se muestra el precio en el sitio** (hero de modelo, modelos similares, catálogo, mega-menú/acordeón, carrusel de versiones, borrador local) + etiquetas internas del Studio de Sanity — consistente con la convención de toyota.mx.
  2. **"Tracción: Delantera FWD" quitado de 5 modelos** (Corolla, Corolla HEV, Yaris Hatchback, Yaris Sedán, Yaris Sedán HEV) por ser un dato idéntico en los 5 (todos los Sedanes & Hatchbacks del catálogo son FWD) sin ningún valor distintivo. Reemplazado por un dato real y distinto por modelo: Corolla → Capacidad de carga (470 KG, verificado en la sección "Corolla en números" de toyota.mx), Corolla HEV → Rendimiento (31.31 KM/L, el más alto del catálogo), Yaris Hatchback/Yaris Sedán → Transmisión (CVT y MT, ambos ofrecen manual a diferencia del Corolla), Yaris Sedán HEV → Modos de manejo (valor acortado a solo "4" tras detectar que el texto completo se desbordaba en la vista móvil de 2 columnas). Nuevo ícono "Modos de manejo" (volante de 3 rayos) agregado a `HighlightIcon.tsx` y al schema.
  3. **Ícono de "Transmisión" rediseñado** — el original (círculo con 8 rayos) parecía un sol, no una transmisión; reemplazado por un engrane real.
  4. **Prius: 2 bugs de contenido corregidos** — la intro no empezaba con la frase en negrita que sí tienen los demás modelos (corregida), y los 6 círculos de "Colores disponibles" se veían en blanco porque los valores `hex` de `coloresExterior` se guardaron sin el símbolo `#` (copiados directo del atributo `data-color1` del DOM de toyota.mx) — corregidos los 6.
  5. **CTA final decía "manejar" en vez de "estrenar"** — el copy correcto ya se había aplicado en `app/preview-borrador/page.tsx` en una revisión de borrador anterior pero nunca llegó a la plantilla real (`app/modelos/[slug]/page.tsx`); corregido para los 24 modelos del catálogo (plantilla compartida).
  6. **Botones del menú móvil agrandados** — los botones de categoría dentro de "Modelos" (Sedanes, SUV's, etc.) tenían un área de toque de ~35-38px (`py-2.5`), por debajo del mínimo recomendado (~44px). Se agrandó el padding (`py-4`) y se agregaron divisores de ancho completo en `Navbar.tsx` y `ModelosMobileAccordion.tsx`, siguiendo el patrón visual del menú móvil de toyota.mx (se evaluó rediseñar todo el menú como overlay de pantalla completa, se descartó por mayor riesgo sin resolver el problema puntual reportado).

  Todos los cambios publicados/subidos con confirmación explícita de Raúl. Ver `CHANGELOG.md` para el detalle completo de cada uno.
- [x] **`yaris-hatchback-tarjeta.webp` reexportada con más aire alrededor del auto** (17 de septiembre de 2026) — comparada en el borrador local del mega-menú, subida como nuevo asset a Sanity y publicada. Se probó el mismo ajuste en `corolla-tarjeta.webp` pero se descartó (quedó igual a la ya publicada, sin cambios). **Lección técnica:** al comparar una imagen local nueva contra la misma URL, el optimizador de imágenes de Next.js puede servir una versión en caché aunque el archivo en disco ya cambió — renombrar el archivo (no solo sobreescribirlo) fuerza una recarga real.
- [x] **Revisión post-publicación del Yaris Sedán — 4 correcciones a la plantilla/copy compartida** (16 de septiembre de 2026, aplican a los 5 modelos publicados): (1) copy sin lenguaje de "renovación" (ya no vigente para este rediseño), corregido en `yaris-sedan.json` y evitado desde el inicio en `yaris-sedan-hev.json`; (2) bug de `aspect-[4/3]` en vez de `aspect-[3/2]` en Destacado y Rendimiento (`app/modelos/[slug]/page.tsx`) y en la Galería grid/lightbox (`components/modelo/Galeria.tsx`) — recortaba/dejaba espacio en blanco de más, corregido en los 4 lugares; (3) disclaimer de precios bajo versiones editado (se quitó "imágenes ilustrativas", tono ajustado); (4) tarjeta de versión más compacta en mobile (`VersionesCarousel.tsx`, ~50px menos de alto por tarjeta) sin tocar tamaño de texto ni botones. Ver `CHANGELOG.md` y Obsidian `catalogo-de-modelos.md` para el detalle completo.
- [x] **`destacadoTexto` migrado a Portable Text** (16 de septiembre de 2026) — antes texto plano, ahora admite viñetas reales (igual que `descripcion`). Corolla y Corolla HEV ya migrados con los 6 componentes reales de Toyota Safety Sense (antes un párrafo genérico). Ver `SISTEMA-DE-DISENO.md` §6.
- [x] **`/preview-borrador` generalizado** (16 de septiembre de 2026) — antes leía siempre `corolla-hev.json` a mano; ahora acepta `?json=<archivo>` y filtra colores placeholder dinámicamente por nombre, reutilizable para cualquier modelo sin tocar código.
- [x] Filtro "Híbridos Eléctricos (HEV y PHEV)" implementado (15 de septiembre de 2026) — campo nuevo `esHibridoElectrico` (booleano, no reemplaza a `categoria`), Corolla HEV marcado y publicado, filtro de `/modelos` corregido. Se quitó "Híbridos Eléctricos" de la lista de valores de `categoria`.
- [x] Imagen hero mobile dedicada (frontal 4:3) vía <picture>
- [x] Viewer 360° real: 7 colores × 16 ángulos — drag/swipe + flechas en escritorio, solo drag/swipe en mobile (22 de agosto: se quitaron los botones de flecha en mobile por ser confusos/redundantes con el gesto de arrastre)
- [x] Revisión de UX mobile (22 de agosto): visor 360°, "colores disponibles" (scrollbar propio en vez de degradado), texto/botón de ficha técnica, botones "Solicitar Cotización" del Corolla (ancho y centrado) — ver `CHANGELOG.md`
- [ ] Pendiente del handoff: PDF ficha técnica
- [ ] Rediseño visual de tarjetas de modelo según wireframe aprobado
- [x] Botones "Cotízalo"/"Manéjalo" en la tarjeta de versión del Corolla (15 de septiembre de 2026) — Propuesta A decidida tras comparar 3 variantes en local; sin destino todavía (`href="#"`), pendiente de que existan los formularios de Cotización (por versión) y Prueba de Manejo — ver `CHANGELOG.md` y Obsidian `formularios-cotizacion-y-prueba-de-manejo.md`
- [ ] Diseño de homepage con hero real (imagen de auto)

### Contenido
- [ ] Cargar los modelos Toyota en Sanity con imágenes y precios — 24 en el catálogo completo de toyota.mx (probablemente un subconjunto real para Cuautitlán), ver Obsidian `catalogo-de-modelos.md` para la lista y estructura completa
- [ ] Vista previa de borradores **ya publicados en Sanity** (Next.js Draft Mode) — pendiente, no urgente todavía. No confundir con `/preview-borrador` (ya construido): ese resuelve revisar copy/specs de un JSON *antes* de tocar Sanity con fotos prestadas; esto otro resolvería ver un borrador que *ya está en Sanity* (ej. un refresh de año-modelo sobre un documento existente) con el diseño real antes de publicar. Hoy eso solo se puede ver en el editor de Sanity Studio (campos sueltos). Construirlo antes del primer refresh de año-modelo (ej. Corolla 2026 → 2027). Ver Obsidian `catalogo-de-modelos.md`.
- [ ] Definir estructura de versiones por modelo (LE, SE, HEV, etc.)
- [ ] Cargar promociones vigentes
- [ ] Llenar Configuración del sitio (teléfono, WhatsApp, horario, redes)

### SEO
- [ ] Sitemap.xml dinámico — esperar a tener el dominio final conectado (no tiene caso construirlo antes)
- [x] robots.txt (22 de agosto de 2026) — `app/robots.ts` bloquea toda indexación (`Disallow: /`) mientras el sitio vive en la URL temporal de Vercel. **CRÍTICO antes del lanzamiento real: quitar el bloqueo (permitir "/"), o Google nunca indexará el sitio.**
- [x] **Schema.org (AutoDealer + Car)** (19 de septiembre de 2026) — a diferencia del sitemap y Open Graph, no dependía del dominio final: `lib/structured-data.ts` arma `AutoDealer` (todo el sitio, desde "Configuración del sitio" en Sanity — hoy vacío, degrada a `name`/`url`) y `Car` por modelo (precio, moneda MXN, imagen, descripción, URL). `components/JsonLd.tsx` los inyecta como `<script type="application/ld+json">`. No se "activa" — ya corre en producción, solo que Google no lo lee hasta quitar el bloqueo de `robots.ts` en el lanzamiento. Se enriquecerá solo en cuanto se llene "Configuración del sitio" (teléfono/dirección/redes), sin tocar código.
- [x] **Meta tags Open Graph por página** (19 de septiembre de 2026) — no dependía del dominio final: `generateMetadata` en `app/modelos/[slug]/page.tsx` genera `openGraph` (title/description/url/image) y `alternates.canonical` para cada modelo, reutilizando `imagenTarjeta` (1200×675, ya casi el estándar 1200×630) como `og:image` sin pedir una imagen nueva. Se aplica solo a cada modelo nuevo conforme se publica.
- [ ] Revalidación de contenido Sanity → sitio en vivo (encontrado 15 de septiembre de 2026): las páginas de modelo son HTML estático sin revalidación — publicar en Sanity no se refleja en Vercel hasta el siguiente deploy. **Aplazado a propósito** (decisión 15 de septiembre): mientras el sitio no tenga visitantes reales, no urge — hoy se resuelve solo, porque cada cierre de sesión empuja un commit a `main` que dispara un deploy nuevo. Antes del lanzamiento real, implementar ISR o un webhook de Sanity → ruta de revalidación en Next.js. Ver `CHANGELOG.md` y `AGENTS.md`.

### Dominio — pospuesto a propósito hasta terminar el desarrollo
- [ ] Conectar toyotacuautitlan.com.mx a Vercel
- [ ] Configurar DNS en el registrador del dominio
- [ ] SSL automático (Vercel lo hace solo)

### Formularios y CRM — pospuesto a propósito hasta terminar el desarrollo
- [ ] Definir qué CRMs conectar en el lanzamiento (Zeenvia, Seekop, SaleU, propio)
- [ ] Integrar API de CRM(s) en /api/cotizacion y /api/cita
- [ ] Notificación por email al equipo de ventas cuando llega un lead

### Flujo de trabajo con equipo
- [x] Compartir .env.local con Raúl por canal seguro
- [x] Repo clonado por Raúl, npm install + npm run dev verificados
- [x] Acordar convención de trabajo (21 de agosto de 2026): rama por tarea + `git pull` antes
      de empezar + registro en CHANGELOG.md — sin Pull Request con aprobación humana obligatoria
      (descartado a propósito, ninguno de los dos puede revisar código del otro). `AGENTS.md` es
      la fuente de verdad de este flujo — cualquier sesión de Claude Code en el repo lo lee solo.
- [x] Regla de diseño (22 de agosto de 2026): consultar `SISTEMA-DE-DISENO.md` antes de cualquier
      decisión de diseño o al crear un componente/sección nueva — documentado en `AGENTS.md`.
- [x] Regla de publicación en Sanity (22 de agosto de 2026): el contenido se guarda como borrador
      y solo se publica (se ve en vivo en el sitio) con confirmación explícita del dueño del
      contenido — documentado en `AGENTS.md`.
- [ ] Confirmar acceso de Raúl a Vercel
- [ ] Asignar secciones del proyecto para no duplicar esfuerzo entre Beto y Raúl

### Precios
- [ ] Definir estructura de versiones por modelo
- [ ] Implementar actualización masiva por CSV o Google Sheets

### Antes del lanzamiento
- [ ] Pruebas en mobile (iOS y Android)
- [ ] Velocidad y Core Web Vitals (objetivo: >90 en PageSpeed)
- [ ] Reemplazar sitio WordPress actual con el nuevo
- [ ] **Quitar el bloqueo de `app/robots.ts`** (permitir "/") — hoy bloquea toda indexación a propósito

### Seguridad — CRÍTICO, pendiente de implementar
- [ ] Rate limiting en formularios de cotización y cita (evitar spam/saturación al CRM)
- [ ] Captcha invisible (ej. Cloudflare Turnstile) en formularios públicos
- [ ] Roles correctos en Sanity al invitar equipo (Editor, no Administrator)
- [ ] Confirmar política de retención/backups de versiones en Sanity
- [ ] Activar Dependabot en GitHub (alertas de vulnerabilidades en dependencias)
- [ ] Revisar permisos de acceso en Vercel al sumar colaboradores

Nota: gran parte de lo básico ya viene resuelto por el stack (sin panel admin público
tipo wp-admin, sin plugins de terceros, SSL automático, CDN de Vercel filtra tráfico
malicioso). Lo de arriba es lo que falta configurar activamente antes de manejar
leads reales de producción.
